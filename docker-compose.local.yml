version: "3.9"
services:
  api_getaway:
    build:
      dockerfile: Dockerfile
      context: .
    command: run
    ports:
      - "5000:5000"
    restart: unless-stopped

networks:
  default:
    name: wrsnet
